<?php

try
    {
	$tryBlock = 1;
    }
catch (Exception $e)
    {
	$catchBlock = 1;
    }

do
    {
	$doBlock = 1;
    } while ($condition === true);

while ($condition === true)
    {
	$whileBlock = 1;
    }

for ($i = 0; $i++; $i < 10)
    {
	$forBlock = 1;
    }

if ($condition === true)
    {
	$ifBlock = 1;
    }
else if ($condition === false)
    {
	$elseifBlock = 1;
    }
else
    {
	$elseBlock = 1;
    }

foreach ($list as $element)
    {
	$foreachBlock = 1;
    }

// Bad constructs

try {
    $tryBlock = 1;
} catch (Exception $e) {
    $catchBlock = 1;
}

do {
    $doBlock = 1;
} while ($condition === true);

while ($condition === true) {
    $whileBlock = 1;
}

for ($i = 0; $i++; $i < 10) {
    $forBlock = 1;
}

if ($condition === true) {
    $ifBlock = 1;
} else if ($condition === false) {
    $elseifBlock = 1;
} else {
    $elseBlock = 1;
}

foreach($list as $element) {
    $foreachBlock = 1;
}

?>